numpy==2.4.2
pandas==2.2.3
plotly==6.2.0
streamlit==1.50.0
qiskit==2.3.0
qiskit-aer==0.17.2
matplotlib==3.10.8

# ── Survey system storage ───────────────────────────────────────────────────
# SQLAlchemy drives both the local SQLite file and production Postgres.
# psycopg2-binary is only used when DATABASE_URL points at Postgres
# (Heroku / DigitalOcean managed DB); harmless to keep installed locally.
SQLAlchemy==2.0.50
psycopg2-binary==2.9.12

# Optional: Google Sheets secondary mirror (admin "Sync" + live append).
# Only used if a service account is configured; harmless to keep installed.
gspread==6.2.1
google-auth==2.53.0
